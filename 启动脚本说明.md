# 项目启动脚本说明

本项目提供了两个启动脚本，让您能够轻松启动和管理所有功能。

## 🚀 快速开始

### 方式一：交互式快速启动（推荐新手）
```bash
./quick_start.sh
```
这个脚本提供友好的菜单界面，适合不熟悉命令的用户。

### 方式二：命令行直接启动（推荐熟练用户）
```bash
./start.sh <命令> [参数...]
```

## 📋 脚本功能对比

| 功能 | quick_start.sh | start.sh |
|------|----------------|----------|
| 交互式菜单 | ✅ | ❌ |
| 完整功能 | ✅ | ✅ |
| 新手友好 | ✅ | ⭐ |
| 自动化脚本 | ❌ | ✅ |
| 参数化控制 | ❌ | ✅ |

## 🛠️ 主要功能

### 1. 项目管理
- **构建项目**: 自动检查依赖、编译代码
- **测试功能**: 验证程序是否正常工作

### 2. 数据采集（需要root权限）
- **快速采集**: 30秒数据采集
- **深度采集**: 60秒数据采集
- **自定义采集**: 可指定时长和频率

### 3. 数据分析
- **一键分析**: 采集+生成火焰图
- **历史查询**: 查询指定时间范围
- **最近数据**: 查询最近N分钟数据

### 4. 数据管理
- **查看信息**: 显示数据库统计信息
- **清理数据**: 删除旧数据文件

## 📖 使用示例

### 新手用户（推荐）
```bash
# 启动交互式菜单
./quick_start.sh

# 选择选项：
# 1) 构建项目
# 2) 快速分析 (30秒)
# 3) 深度分析 (60秒)
# 4) 查看现有数据
# 5) 清理旧数据
# 6) 显示帮助
```

### 熟练用户
```bash
# 构建项目
./start.sh build

# 一键分析（推荐）
sudo ./start.sh analysis 60 100

# 查看数据库信息
./start.sh info data/perf.db

# 生成火焰图
./start.sh query data/perf.db

# 清理7天前的数据
./start.sh cleanup 7
```

## 🔧 高级用法

### 自定义采集参数
```bash
# 采集120秒，200Hz频率
sudo ./start.sh collect 120 200

# 查询指定时间范围
./start.sh query data/perf.db 1640995200 1640995800
```

### 批量处理
```bash
# 循环采集和分析
for i in {1..5}; do
    echo "第 $i 轮分析..."
    sudo ./start.sh analysis 60 100
    sleep 300  # 休息5分钟
done
```

## ⚠️ 注意事项

1. **权限要求**: 数据采集需要root权限
2. **依赖检查**: 脚本会自动检查系统依赖
3. **参数建议**: 
   - 频率: 30-200Hz
   - 时长: 30-300秒
4. **文件位置**: 数据保存在 `data/` 目录

## 🆘 故障排除

### 问题1: 权限不足
```bash
❌ 错误: 此操作需要 root 权限
```
**解决**: 使用 `sudo` 运行采集命令

### 问题2: 依赖缺失
```bash
❌ 错误: 缺少以下依赖: gcc make libelf-dev
```
**解决**: 运行脚本会自动提示安装命令

### 问题3: 编译失败
**解决**: 检查依赖是否完整安装，运行 `./start.sh build` 查看详细错误

## 📁 文件结构

```
perf/
├── start.sh              # 主启动脚本（完整功能）
├── quick_start.sh        # 快速启动脚本（交互式）
├── my_elf_reader         # 核心程序
├── data/                 # 数据目录
│   ├── perf_*.db         # 数据库文件
│   └── analysis_*.svg    # 火焰图文件
└── 启动脚本说明.md       # 本文档
```

## 🎯 最佳实践

1. **首次使用**: 运行 `./quick_start.sh` 选择"构建项目"
2. **日常分析**: 使用 `sudo ./start.sh analysis 60 100`
3. **查看结果**: 在浏览器中打开生成的 `.svg` 文件
4. **定期清理**: 运行 `./start.sh cleanup 7` 清理旧数据

## 📞 获取帮助

```bash
# 查看完整帮助
./start.sh help

# 查看项目文档
cat 使用文档.md
```

---

**版本**: 1.0  
**更新时间**: 2025-01-06  
**兼容性**: Linux (WSL2支持)

