# 性能分析工具使用文档

基于 Linux perf 的性能采集与离线分析工具，支持历史火焰图回溯。

---

## 快速开始（3步上手）

### 1. 构建项目
```bash
./perf_tool.sh build
```

### 2. 采集并分析（推荐）
```bash
sudo ./perf_tool.sh analysis 60 100
```
采集60秒数据并自动生成火焰图。

### 3. 查看结果
在浏览器中打开生成的 `.svg` 文件。

---

## 命令列表

```bash
./perf_tool.sh <命令> [参数...]
```

### 数据采集
- **`collect [时长] [频率]`** - 快速采集性能数据
  ```bash
  sudo ./perf_tool.sh collect 30 100
  ```

- **`analysis [时长] [频率]`** - 一键分析（推荐）
  ```bash
  sudo ./perf_tool.sh analysis 60 100
  ```

### 数据查询
- **`query <数据库> [开始] [结束] [输出]`** - 查询并生成火焰图
  ```bash
  ./perf_tool.sh query data/perf.db
  ```

- **`recent <数据库> [分钟] [输出]`** - 生成最近N分钟火焰图
  ```bash
  ./perf_tool.sh recent data/perf.db 10
  ```

- **`batch <数据库> [间隔]`** - 批量生成火焰图
  ```bash
  ./perf_tool.sh batch data/perf.db 600
  ```

### 数据管理
- **`info <数据库>`** - 查看数据库详细信息
  ```bash
  ./perf_tool.sh info data/perf.db
  ```

- **`cleanup [天数]`** - 清理N天前的旧数据
  ```bash
  ./perf_tool.sh cleanup 7
  ```

### 项目管理
- **`build`** - 构建项目
- **`test`** - 测试项目
- **`help`** - 显示帮助

---

## 常用场景

### 场景1：快速性能诊断
```bash
# 一键采集并生成火焰图（最简单）
sudo ./perf_tool.sh analysis 30 100
```

### 场景2：深度性能分析
```bash
# 步骤1：采集数据
sudo ./perf_tool.sh collect 300 100

# 步骤2：查看统计
./perf_tool.sh info data/perf_*.db

# 步骤3：生成火焰图
./perf_tool.sh query data/perf_*.db
```

### 场景3：分析历史数据
```bash
# 查看数据库时间范围
./perf_tool.sh info data/perf.db

# 生成特定时间段火焰图（使用Unix时间戳）
./perf_tool.sh query data/perf.db 1759594110 1759594200 data/target.svg
```

### 场景4：批量分析
```bash
# 按10分钟间隔批量生成火焰图
./perf_tool.sh batch data/perf.db 600
```

---

## 参数建议

### 采样频率
| 频率 | 场景 | CPU开销 |
|------|------|--------|
| 30-50 Hz | 长期监控 | 很低 |
| 100 Hz | 常规分析（推荐） | 低 |
| 200 Hz | 深度分析 | 中等 |

### 采集时长
| 时长 | 场景 |
|------|------|
| 30-60秒 | 快速诊断 |
| 60-180秒 | 常规分析（推荐） |
| 300-600秒 | 深度分析 |

---

## 底层命令（高级用法）

如果统一脚本不满足需求，可直接使用底层命令：

### 采集
```bash
sudo ./my_elf_reader collect \
    --output data/perf.db \
    --frequency=100 \
    --stack-depth=127 \
    --filter=all
```

参数说明：
- `--frequency=<Hz>` - 采样频率（默认30）
- `--stack-depth=<n>` - 栈深度（默认127）
- `--filter=<mode>` - 过滤模式：all|user|kernel
- `--lbr` - 启用LBR（更精确但开销更大）

### 查询
```bash
./my_elf_reader query \
    --input data/perf.db \
    --from 1759594110 \
    --to 1759594200 \
    --flamegraph data/flame.svg
```

---

## 数据文件说明

### 数据库文件
```
data/perf_20251005_235424.db      # SQLite数据库
data/perf_20251005_235424.db-wal  # 预写日志
```

**表结构**：
- `raw_samples` 表：存储原始调用栈
- 字段：`id`, `timestamp`, `pid`, `tid`, `callstack`
- 索引：`idx_timestamp`

**查询示例**：
```bash
# 样本总数
sqlite3 data/perf.db "SELECT COUNT(*) FROM raw_samples;"

# 时间范围
sqlite3 data/perf.db "SELECT MIN(timestamp), MAX(timestamp) FROM raw_samples;"

# 进程分布
sqlite3 data/perf.db "SELECT pid, COUNT(*) FROM raw_samples GROUP BY pid ORDER BY COUNT(*) DESC LIMIT 10;"
```

### 火焰图文件
```
data/analysis_20251005_235424.svg  # 交互式SVG
```

**查看方式**：
```bash
# Linux
xdg-open data/analysis_*.svg

# Windows WSL
explorer.exe data/analysis_*.svg

# 或直接在浏览器中打开
```

---

## 故障排除

### 问题1：权限不足
```
❌ 错误: 此操作需要 root 权限
```
**解决**：使用 `sudo` 运行采集命令

### 问题2：样本数过低
```
Stack count is low (29). Did something go wrong?
```
**解决**：增加采集时长或频率
```bash
sudo ./perf_tool.sh collect 120 200
```

### 问题3：编译错误
```
fatal error: sqlite3.h: No such file or directory
```
**解决**：安装依赖
```bash
# Ubuntu/Debian
sudo apt-get install gcc make libelf-dev libsqlite3-dev

# 重新编译
./perf_tool.sh build
```

### 问题4：磁盘空间不足
**解决**：清理旧数据
```bash
./perf_tool.sh cleanup 3
```

---

## 自动化示例

### 定时采集（crontab）
```bash
# 每小时采集5分钟
0 * * * * cd /path/to/perf && sudo ./perf_tool.sh collect 300 50
```

### 批处理脚本
```bash
#!/bin/bash
# 循环采集和分析

for i in {1..5}; do
    echo "第 $i 轮采集..."
    sudo ./perf_tool.sh collect 300 100
    
    DB=$(ls -t data/perf_*.db | head -1)
    ./perf_tool.sh query "$DB"
    
    sleep 600  # 休息10分钟
done
```

---

## 最佳实践

### ✅ 推荐
- 先测试后部署
- 使用合理的采样频率（避免过高）
- 定期清理旧数据
- 备份重要数据
- 使用统一脚本 `perf_tool.sh`

### ❌ 避免
- 在生产环境使用过高频率（>500Hz）
- 忽略磁盘空间
- 在高峰期进行深度分析

---

## 项目架构

```
perf/
├── my_elf_reader          # 核心程序
├── perf_tool.sh           # 统一管理脚本（推荐使用）
├── 使用文档.md            # 本文档
├── data/                  # 数据目录
│   ├── *.db              # 数据库文件
│   └── *.svg             # 火焰图文件
├── src/                   # 源代码
├── include/               # 头文件
└── third_party/           # 第三方库（flamegraph.pl）
```

---

## 功能特性

- ✅ 低开销实时采集
- ✅ 离线符号化与聚合
- ✅ 历史火焰图回溯
- ✅ 批量事务写入优化
- ✅ 时间范围灵活查询
- ✅ 交互式火焰图输出

---

## 测试验证

项目已通过完整测试：
- ✅ 编译成功（无警告）
- ✅ 功能测试通过
- ✅ 数据采集正常（48,058样本测试）
- ✅ 火焰图生成正常
- ✅ 查询性能优秀（秒级）

---

## 命令速查表

| 命令 | 功能 | 需要root | 示例 |
|------|------|---------|------|
| `analysis` | 一键分析 | ✅ | `sudo ./perf_tool.sh analysis 60 100` |
| `collect` | 快速采集 | ✅ | `sudo ./perf_tool.sh collect 30 100` |
| `query` | 查询生成 | ❌ | `./perf_tool.sh query data/perf.db` |
| `recent` | 最近时段 | ❌ | `./perf_tool.sh recent data/perf.db 10` |
| `batch` | 批量生成 | ❌ | `./perf_tool.sh batch data/perf.db 600` |
| `info` | 查看信息 | ❌ | `./perf_tool.sh info data/perf.db` |
| `cleanup` | 清理数据 | ❌ | `./perf_tool.sh cleanup 7` |
| `build` | 构建项目 | ❌ | `./perf_tool.sh build` |
| `test` | 测试项目 | ❌ | `./perf_tool.sh test` |
| `help` | 显示帮助 | ❌ | `./perf_tool.sh help` |

---

**文档版本**: 1.0  
**更新时间**: 2025-10-06

